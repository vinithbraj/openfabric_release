"""Compatibility runtime package for legacy run/session/CLI surfaces.

Purpose:
    Preserve older envelopes such as ``/runs`` and ``/sessions`` while routing
    them into the current typed agent runtime.
"""
